{
  "name": "totp-decoder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "dev": "cd web && npm run dev",
    "install-deps": "cd web && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}